[project]
name = "project-q"
version = "0.1.0"
description = "Windows-first personal AI executive agent foundation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "cryptography>=45.0.0,<47.0.0",
  "pydantic>=2.0.0",
  "qrcode[pil]>=8.0.0,<9.0.0",
]

[tool.pyright]
include = ["src", "tests"]
